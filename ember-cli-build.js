/* eslint-env node */
const EmberApp = require('ember-cli/lib/broccoli/ember-app');

module.exports = function(defaults) {
  var app = new EmberApp(defaults, {
    // Add options here
  });

  // Use `app.import` to add additional libraries to the generated
  // output files.
  //
  // If you need to use different assets in different
  // environments, specify an object as the first parameter. That
  // object's keys should be the environment name and the values
  // should be the asset to use in that environment.
  //
  // If the library that you are including contains AMD or ES6
  // modules that you would like to import into your application
  // please specify an object with the list of modules as keys
  // along with the exports of each module as its value.
  // AWS API Gateway JavaScript SDK Deps

  // https://github.com/material-components/material-components-web
  app.import('vendor/material-components-web/material-components-web.min.css');
  app.import('vendor/material-components-web/material-components-web.min.js');
  
  // https://github.com/aws/amazon-cognito-identity-js
  app.import('vendor/amazon-cognito/aws-cognito-sdk.min.js');
  app.import('vendor/amazon-cognito/amazon-cognito-identity.min.js');
  
  // https://github.com/auth0/jwt-decode
  app.import('vendor/jwt-decode.min.js');
  
  // CP'd from S3; files generated by MobileHub
  app.import('vendor/aws-sdk.min.js');
  app.import('vendor/aws-config.js');
  
  return app.toTree();
};
